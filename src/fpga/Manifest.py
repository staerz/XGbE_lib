# Additional module options: compile files locally
# mod_opt = {'vcom_vlog_library_path': '.'}

# List of modules
modules = {
  'local' : [
    'context',
    {'sim': {'action': 'simulation'}},
    'syn',
  ],
}
